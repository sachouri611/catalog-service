// Canonical list of analyzable file extensions.
// Owned by the providers layer: it is the adapter that knows
// which external formats the system can ingest.
export const ANALYZABLE = ['.ts', '.tsx', '.js', '.jsx', '.mjs', '.cjs'];
